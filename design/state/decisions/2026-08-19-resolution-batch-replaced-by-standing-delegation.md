# decision/2026-08-19-resolution-batch-replaced-by-standing-delegation
Date: 2026-08-19
Anchor: 2026-08-19 — The resolution batch is replaced by standing delegation; I3 and I4 are retired
Status: accepted

## Claim
There is no authorization batch. `/resolve` pushes the fix and resolves every `Defect`-class thread that fix satisfies without asking first, under `AGENTS.md`, *Git and delivery*; `Ambiguous` threads are still brought individually, and in a repository the account does not own every action reverts to an individual ask (I9). I3 and I4 are retired — their ids stay resolvable and they leave every closure — and I9 is reworded to name the delegation rather than the batch. A retired invariant also leaves `design/20-contract.md` § *Invariants*, which is the invariant unit set rather than a history.
