# I32
Kind: invariant
Status: active
Anchor: I32
Enforcement: code
Consumes:
Exposes:
Binds:
Live:
Archival:
Questions:
Work:
Evidence: tools/Merge-PullRequest.Tests.ps1

## Statement
`Merge-PullRequest.ps1` merges only an open, non-draft pull request whose head is exactly the SHA
named to it, whose every check reached a terminal passing state on that SHA, and whose review
threads are all resolved. Every condition it cannot positively confirm — an unreadable API, a
timeout, an unrecognised bucket, or a repository with no checks configured at all — refuses.
