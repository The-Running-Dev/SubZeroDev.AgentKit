@AGENTS.shared.md
@AGENTS.md

Everything in `AGENTS.shared.md` and `AGENTS.md` applies. Together they are the agent contract for this repo — the part every kit repository shares, then this repository's own project rules; this file exists only so Claude Code loads them.

If an `@` import is not resolving in your version, a hardlink no longer covers the contract, because it is two files. Say so rather than pasting either file into this one — a pasted copy is a second home for every rule in it.
